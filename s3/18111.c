#include <stdio.h>

int main (void)
{
    int N, M, B;
    scanf("%d %d %d", &N, &M, &B);
    int arr[N*M];
    for(int i=0; i<N*M; i++)
    {
        scanf("%d", &arr[i]);
    }
    
    

    return 0;
}